# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    environment: prod
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'yarn build:prod'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FORCED_EXIT_ROPSTEN }}'
          channelId: live
          projectId: zksync-forced-exit-ropsten
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
    
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FORCED_EXIT_RINKEBY }}'
          channelId: live
          projectId: zksync-forced-exit-rinkeby
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
        
      
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FORCED_EXIT_MAINNET }}'
          channelId: live
          projectId: zksync-forced-withdrawal
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels